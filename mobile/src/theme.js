/**
 * 색·간격 토큰.
 *
 * 화재 상황에서 쓰는 앱이라 대비를 높게 잡았다. 저시력자가 큰 글씨와
 * 강한 대비만으로도 상태를 읽을 수 있어야 하고, 연기 속에서 화면이
 * 흐려져도 초록/빨강 구분은 남아야 한다.
 */
export const theme = {
  bg: '#0b0f14',
  surface: '#151b23',
  border: 'rgba(255,255,255,0.35)',

  text: '#f3f4f6',
  textDim: '#9ca3af',

  accent: '#2563eb',
  ok: '#22c55e',
  warn: '#f59e0b',
  danger: '#ef4444',

  radius: 16,
  gap: 12,

  /**
   * 지도 색 — **관제(`frontend/admin.html`)와 같은 값.**
   *
   * 앱과 관제가 같은 도면을 다른 색으로 그리면, 둘을 나란히 놓고 맞춰 보는
   * 일(답사·시연)이 어려워진다. 무엇보다 «관제에서 파란 선» 과 «폰에서 초록 선»
   * 이 같은 것이라는 걸 사람이 따로 외워야 한다.
   *
   * 색의 뜻도 관제와 같게 지킨다:
   *   빨강   화재 — 이 색은 불에만 쓴다
   *   주황   주의 — 봐야 하지만 위험은 아닌 것
   *   파랑   경로·현재
   *   민트   출구·안전
   */
  map: {
    corridor: '#6b7480',   // --corridor
    route: '#4d9fff',      // --route
    node: '#97a2ae',       // --node
    exit: '#2fd6a6',       // --ok
    text: '#b9c4d0',       // --map-text
    danger: '#ff4438',     // --danger
    warn: '#e79a3c',       // --warn
    // 비콘은 **보라**다. 예전에는 출구와 같은 민트였는데, 지도에서 «저 초록 점이
    // 출구인가 비콘인가» 가 안 갈렸다. 색이 뜻을 하나씩만 맡아야 흘깃 봐도 읽힌다.
    beacon: '#b06bff',     // --beacon
  },
};
