# CLAUDE.md

프로젝트 개요·실행법·API·데모 시나리오는 **README.md**가 단일 진실 소스다. 여기에는
README에 없는 **설계 결정**만 적는다.

## 측위 방식 결정 (2026-08): BLE 비콘 최근접 판정

현재 위치(`fromId`)는 **BLE 비콘 최근접 판정**으로 얻는다.
경로 계산은 이미 완성돼 있으므로 (`shared/pathfinding.js`의
`routeToNearestExit(floorPlan, fromId, hazards)`), 측위 계층의 유일한 역할은
`fromId` 하나를 공급하는 것이다.

### 원리

- 그래프 노드(실·교차점·출구)에 비콘을 배치하고 `beaconId → nodeId` 매핑을 도면 데이터에 둔다.
- 폰이 BLE 스캔으로 받은 RSSI 중 **가장 센 비콘의 노드 = 현재 위치**로 판정한다.
- RSSI를 거리 측정에 쓰면 실내 반사·인체 감쇠로 오차 3~5m라 실패하지만,
  "어느 비콘이 제일 가까운가" **비교**에만 쓰면 오차가 문제되지 않는다.
  대피 안내에 필요한 해상도는 노드 단위("302호 앞")이고, 이 판정이 정확히 그 해상도다.

### 반드시 넣어야 하는 처리

- **스무딩**: 원시 RSSI는 정지 상태에서도 ±10dBm 튄다. 3초 이동평균으로 평활.
- **히스테리시스**: 현재 노드를 바꿀 때는 새 비콘이 기존보다 **5dB 이상 세게, 2초 지속**될 때만
  전환한다. 없으면 노드 경계에서 위치가 초 단위로 오락가락해 경로가 화면에서 깜빡인다.
- **그래프 제약**: 현재 노드에서 엣지로 연결되지 않은 노드로의 순간이동 판정은 무시한다.

### 구현 상태 (2026-08-14)

| 파일 | 역할 |
|---|---|
| `shared/positioning.js` | `BeaconLocator` — 스캔 배열 → nodeId. 스무딩·히스테리시스·그래프 제약 |
| `shared/beacon-sim.js` | 가상 비콘 — 거리 기반 RSSI + 노이즈. 실제 스캔과 같은 형식을 낸다 |
| `frontend/js/beacon.js` | `BeaconSense` — 주기 스캔 → 판정 → 앱에 통지 |
| `test/positioning.test.mjs` | 합성 RSSI 시퀀스 검증 (`npm test`에 포함) |

측위 계층은 **시간을 인자로 받고 Date.now()를 부르지 않는다.** 테스트가 시간을
감을 수 있어야 히스테리시스를 검증할 수 있기 때문이다.

`BeaconSense.onLocate`는 지점이 바뀔 때뿐 아니라 **확정된 순간**에도 발화한다.
서 있는 자리에서 처음 잡은 지점이 그대로 확정되는 것이 정상 경로인데,
지점 변화만 보면 그 순간을 놓쳐 영영 알리지 않는다.

### 가상 비콘 — 도면에 비콘 id가 없을 때

비콘을 아직 안 단 도면이면 지점마다 `SIM-<nodeId>` 비콘이 있다고 치고 돌린다
(`withVirtualBeacons`, 엘리베이터 제외). 도면을 올릴 때마다 편집기로 돌아가
비콘 id를 타이핑해야 한다면 아무도 이 기능을 켜보지 않고, 실제 설치 전에
"달면 이렇게 된다"를 먼저 보여줄 수 있어야 설치 여부를 판단할 수 있다.
화면에는 `(가상 비콘 시뮬레이션)`으로 표시해 실측과 구분한다.

### 시각장애인 관점의 값어치

**이 기능의 목적은 "지금 어디 계신가요"를 묻지 않는 것이다.** 목록에서 자기 방
이름을 찾는 일은 눈으로는 1초지만 스크린리더로는 수십 번의 탭이고, 화재
상황에서 그 시간을 쓰게 해서는 안 된다. 그래서 시작 화면은 비콘 판정을
`aria-live`로 즉시 읽어주고 대피 시작 버튼을 열어준다. QR·목록 선택은
비콘이 없는 건물을 위한 폴백으로 `<details>` 안에 접어 두었다.

확정 전(`locked=false`) 잠정값은 **알리지 않는다.** 첫 몇 초의 값은 흔들릴 수
있는데 그때마다 음성이 나가면 "여기다 저기다" 하는 꼴이 된다.

### 데모 구성 (비용 0원)

비콘은 사지 않는다 — **공기계 안드로이드 폰 + Beacon Simulator 앱**(또는 ESP32)이 비콘이다.
데모 구역은 복도 하나로 좁히고 경로를 따라 3개를 배치한다:

```
[302호 앞]────[교차점]────[출구]
  비콘 1        비콘 2      비콘 3
```

- 출구 1개: 도착 판정 + RSSI 기반 "출구까지 몇 m" 표시
- 경유 노드 2개: 걸어가면 현재 위치 마커가 경로를 따라 갱신되는 장면을 만든다
  (비콘이 3개는 되어야 "위치 파악 → 경로 표시 → 이동하며 갱신" 흐름이 영상에 담긴다)

**출구에만 비콘을 두는 구성은 안 된다** — 건물 안쪽에서 신호가 없거나 다 비슷하게 약해
경로의 출발점을 잡을 수 없다. 비콘은 경로의 노드마다 둔다.

### 수신 제약

Web Bluetooth **Scanning** API(광고 수신)는 안드로이드 크롬에서 `chrome://flags`
실험 플래그 뒤에 있고, iOS Safari는 Web Bluetooth 자체가 없다. 데모는 플래그를 켠
안드로이드 크롬 1대로 진행한다. 대안: RSSI를 백엔드로 중계하는 초소형 네이티브 앱
또는 ESP32 스캐너.

### 검토 후 배제한 대안 (발표 시 비교 근거로 사용)

| 방식 | 배제 이유 |
|---|---|
| Wi-Fi RTT (802.11mc) | 정확도 1~2m로 가장 좋지만 **iOS 0%**, 안드로이드도 Pixel 위주. 802.11mc 지원 AP 교체 + 배선 공사에 수백만 원. 유선 전원 AP라 화재 정전 시 측위 전체가 죽는다 — 대피 시스템 인프라로 부적합 |
| RSSI 삼각측량 | 신호세기→거리 환산이 실내 반사·감쇠로 3~10m 오차. 교과서용 |
| Wi-Fi 핑거프린팅 | 정확도 1~3m(Anyplace 방식)지만 사전 신호 측정이 노동집약적이고 환경 변화마다 재측정. 노드 단위 해상도에는 과투자 |
| UWB | 10~30cm 정확도지만 앵커 고가, 지원 폰 제한, 웹 API 없음 |
| QR 수동 체크인 | 확실하지만 사용자 행동 필요. 초기 위치 확정·데모 폴백용으로는 유효 |

보조 수단으로 **PDR(걸음감지·방위추적, `frontend/js/odometry.js`에 이미 있음)**을
비콘 사이 구간을 메우는 데 쓴다: 비콘이 잡히면 그 노드로 위치를 확정(누적오차 리셋)하고,
비콘 사이에서는 걸음 수·방향으로 추정 위치를 이어간다.
