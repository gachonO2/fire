node_modules/
.env
backend/serviceAccountKey.json

# shared/ 에서 자동 복사되는 사본 (npm run sync)
frontend/shared/

# 참고용으로 클론한 오픈소스 원본
vendor/

# 학습 중간 산출물 — 체크포인트가 수십 개씩 쌓여 저장소에 두지 않는다.
# ml/detector/models/*.pt 는 예외다: 추론에 실제로 쓰는 최종 가중치 두 벌(11MB)이라
# 이게 없으면 받아서 바로 돌려볼 수가 없다. ml/detector/README.md 참고.
ml/runs/
ml/**/__pycache__/
ml/*.pt
ml/*.onnx
ml/data_colab.yaml
ml/detector/.venv/
ml/detector/uploads/
ml/detector/outputs/

# 코랩 업로드용으로 묶은 임시 꾸러미
ml-colab.zip
evacuation_mvp_full_with_models.zip

# 확인용으로 꺼낸 촬영 사진 (원본은 backend/data/plans.json 안에 있다)
촬영도면/
